git remote -v (check whether not adding company related things)
git add .
git commit -m "Solved Word Search II using Trie + DFS"
git push

Command	        | Purpose
----------------|---------------------
pwd	confirm     | current folder
git status	    | see tracked files
git remote -v	  | verify GitHub repo
git ls-files	  | inspect all tracked files